name: zync_app
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # --- DEPENDENCIAS DE ARQUITECTURA ---
  # Manejo de estado (Usando Riverpod para coincidir con el código de UI existente)
  flutter_riverpod: ^2.5.1
  
  # Inyección de dependencias
  get_it: ^7.7.0
  
  # Comparación de objetos
  equatable: ^2.0.5
  
  # Programación funcional (para Either)
  dartz: ^0.10.1
  
  # --- DEPENDENCIAS EXTERNAS (CAPA DE DATOS) ---
  # Firebase (versiones estables y compatibles)
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.2.0 # <--- AÑADIDO
  
  # Almacenamiento local
  shared_preferences: ^2.2.3
  
  # Conectividad de red
  connectivity_plus: ^6.1.5
  
  # Iconos
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  change_app_package_name: ^1.1.0 

flutter:
  uses-material-design: true