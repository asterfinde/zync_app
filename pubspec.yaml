name: zync_app
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # Actualizado para ser compatible con versiones recientes

dependencies:
  flutter:
    sdk: flutter

  # --- DEPENDENCIAS DE ARQUITECTURA ---
  # Manejo de estado
  flutter_bloc: ^9.1.1
  meta: ^1.12.0
  
  # Inyección de dependencias
  get_it: ^8.2.0
  
  # Comparación de objetos
  equatable: ^2.0.5
  
  # Programación funcional (para Either)
  dartz: ^0.10.1
  
  # --- DEPENDENCIAS EXTERNAS (CAPA DE DATOS) ---
  # Firebase (versiones estables y compatibles)
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  
  # Almacenamiento local
  shared_preferences: ^2.2.2
  
  # Conectividad de red
  connectivity_plus: ^6.1.5
  
  # Iconos
  cupertino_icons: ^1.0.6
  bloc: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  change_app_package_name: ^1.1.0 

flutter:
  uses-material-design: true
