{
  "name": "firestore-rules-tests",
  "private": true,
  "version": "1.0.0",
  "description": "Tests de Firestore Security Rules contra el Firebase Emulator. No forma parte del build de la app — solo verificacion de reglas antes de desplegar (Sem 9).",
  "type": "module",
  "scripts": {
    "test": "node --test"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.0.0"
  }
}
