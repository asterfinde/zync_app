import 'package:flutter/material.dart';

// TODO(sem5-dia3): implementar — ver in_circle_view.dart líneas 969–1046 (_buildFooterButton)
class InCircleFooter extends StatelessWidget {
  const InCircleFooter({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
